#pragma once

#include <string>
#include <vector>
#include <sstream>

namespace ash {

int find_rate(const std::string& str);

std::vector<std::string> every_valve(const std::string& str);


} // namespace ash

